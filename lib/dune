(library
 (name affinescript)
 (public_name affinescript)
 (libraries str sedlex fmt)
 (preprocess
  (pps ppx_deriving.show ppx_deriving.eq ppx_deriving.ord sedlex.ppx)))

; Parser will be added when parser.mly is implemented
; (menhir
;  (modules parser)
;  (flags --explain --dump))
