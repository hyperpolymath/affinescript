// Test binary operators and precedence
fn test_ops() -> Int {
  1 + 2 * 3
}

fn test_compare() -> Bool {
  x == y && a < b
}

fn test_bitwise(a: Int, b: Int) -> Int {
  (a & b | a ^ b) << 2 >> 1
}

fn test_bitwise_not(n: Int) -> Int {
  ~n
}
